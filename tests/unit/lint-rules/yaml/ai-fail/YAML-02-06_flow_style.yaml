---
# YAML-02-06: Prefer block style over flow style (FAIL)
# AI Review: Overuses flow style, reducing readability

# Bad: Flow style for complex structures
database: {host: localhost, port: 5432, credentials: {username: admin, password: secret}}

# Bad: Flow style for lists with nested data
servers: [{name: web-server-1, ip: 192.168.1.10, roles: [frontend, api]}, {name: web-server-2, ip: 192.168.1.11, roles: [frontend]}]

# Bad: Mixed flow style in config
logging: {level: info, handlers: [{type: file, path: /var/log/app.log}, {type: console, format: json}]}

# Bad: Long flow style line
environment: {DEBUG: true, LOG_LEVEL: debug, DATABASE_URL: postgres://localhost:5432/db, REDIS_URL: redis://localhost:6379, SECRET_KEY: very-long-secret-key-here}
