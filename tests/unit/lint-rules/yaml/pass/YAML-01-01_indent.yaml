# YAML-01-01: 2-space indentation - PASS
# Tool: yamllint (indentation)
---
server:
  host: localhost
  port: 8080
  options:
    timeout: 30
    retries: 3

database:
  primary:
    host: db.example.com
    port: 5432
  replica:
    host: replica.example.com
    port: 5432

features:
  - name: feature1
    enabled: true
  - name: feature2
    enabled: false
