# YAML-04-01: Space after comment hash - PASS
# Tool: yamllint (comments)
---
# This is a proper comment
server:
  host: localhost  # Inline comment with space
  port: 8080  # Another proper comment

# Section comment
database:
  # Connection settings
  host: db.example.com
  port: 5432  # Default PostgreSQL port
