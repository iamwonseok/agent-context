# YAML-01-01: 2-space indentation - FAIL (uses 4 spaces)
# Tool: yamllint (indentation)
---
server:
    host: localhost
    port: 8080
    options:
        timeout: 30
        retries: 3

database:
    primary:
        host: db.example.com
        port: 5432
