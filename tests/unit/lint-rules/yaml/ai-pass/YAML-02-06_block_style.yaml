---
# YAML-02-06: Prefer block style over flow style (PASS)
# AI Review: Uses block style for readability

# Good: Block style for complex structures
database:
  host: localhost
  port: 5432
  credentials:
    username: admin
    password: secret

# Good: Block style for lists
servers:
  - name: web-server-1
    ip: 192.168.1.10
    roles:
      - frontend
      - api
  - name: web-server-2
    ip: 192.168.1.11
    roles:
      - frontend

# Good: Block style for nested config
logging:
  level: info
  handlers:
    - type: file
      path: /var/log/app.log
    - type: console
      format: json
