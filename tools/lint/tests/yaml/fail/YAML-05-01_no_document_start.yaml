# YAML-05-01: Document start marker - FAIL (missing ---)
# Tool: yamllint (document-start)
name: myapp
version: "1.0.0"
description: "Application without document start marker"

config:
  debug: false
  log_level: info
