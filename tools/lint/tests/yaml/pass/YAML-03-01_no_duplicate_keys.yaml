# YAML-03-01: No duplicate keys - PASS
# Tool: yamllint (key-duplicates)
---
config:
  name: myapp
  version: "1.0.0"
  description: "Application description"

server:
  host: localhost
  port: 8080

logging:
  level: info
  format: json
